export default function Dashboard() {
  return (
    <div>
      <h2>Dashboard</h2>
      <p>Here you can show counts from `/api/proxy/active`, user info from `/api/auth/me`, etc.</p>
    </div>
  );
}
